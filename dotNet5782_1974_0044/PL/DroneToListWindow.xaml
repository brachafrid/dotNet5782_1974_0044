<UserControl x:Class="PL.DroneToListWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="Dictionary.xaml"/>
    </UserControl.Resources>
    <DockPanel DataContext="{Binding}">
        <ComboBox x:Name="selectCategory"  SelectionChanged="select_screen_out" >
            <ComboBoxItem Content="weight" />
            <ComboBoxItem Content="state" /> 
        </ComboBox>
        <ComboBox x:Name="ChooseWeight"  ItemsSource="{Binding}" Visibility="Collapsed" SelectionChanged="select_screen_out_parameter"/>
        <ComboBox x:Name="ChooseState" ItemsSource="{Binding}" Visibility="Collapsed" SelectionChanged="select_screen_out_parameter" />
        <Button Content="to cansel screen out"  Click="Cancel_screen_out" Height="10" Width="10"/>
        <ListView x:Name="droneToListListView" ItemsSource="{Binding }"  SelectionMode="Single" DockPanel.Dock="Top"  MouseDoubleClick="double_click" >
        
        </ListView>
    </StackPanel>
</UserControl>
