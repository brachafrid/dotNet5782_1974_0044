<UserControl x:Class="PL.CustomerWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:CustomerWindowVM/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary Source="../Dictionary.xaml" />
    </UserControl.Resources>
   
    <Grid>
        <TextBlock Text="welcome customer" Height="100"/>
        <!--<TextBlock Text="My Parcels: " />-->

        <!--<StackPanel Visibility="{Binding VisibilityParcelsList.visibility}">-->
            <!--<ListView x:Name="listParcels1" ItemsSource="{Binding Path=list}" Margin="0,21,0,-21" />-->
        <TextBlock x:Name="listParcels" Text="{Binding Path=list}"  />
        <Button Content="display my parcels" Command="{Binding DisplayParcelsCommand}"/>
        <!--<Button Content="update" Command="{Binding DisplayParcelsCommand}"/>-->
    </Grid>
</UserControl>
