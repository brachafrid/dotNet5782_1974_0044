<UserControl x:Class="PL.UpdateCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:UpdateCustomerVM/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <!--<ResourceDictionary>
            <local:parcelTreatedByDroneVisibility x:Key="parcelTreatedByDroneVisibility" />
            <local:ConverterUpdateModel x:Key="ConverterUpdateModel" />
            <local:parcelTreatedByDroneContent x:Key="parcelTreatedByDroneContent" />
            <local:ConvertDroneChargingVisibility x:Key="ConvertDroneChargingVisibility" />
            <local:ConvertDroneChargingContent x:Key="ConvertDroneChargingContent" />
        </ResourceDictionary>-->
    </UserControl.Resources>
    <Grid>
        <StackPanel x:Name="details" >
            <TextBlock Text="Id: "/>
            <TextBox x:Name="Id" Text ="{Binding customer.Id, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <TextBlock Text="Name: "/>
            <TextBox x:Name="CustomerName" Text ="{Binding customer.Name, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <TextBlock Text="Phone: "/>
            <TextBox  Text ="{Binding customer.Phone, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <TextBlock Text="Location: "/>
            <TextBlock Text="Latitude: "/>
            <TextBlock  Text ="{Binding customer.Location.Latitude, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <TextBlock Text="Longitude: "/>
            <TextBlock  Text ="{Binding customer.Location.Longitude, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <Button Content="close customer"/>
            <Button Content="update customer" Command="{Binding UpdateCustomerCommand}"/>
            <!--<Button Content="update drone's model" Command="{Binding UpdateDroneCommand}">
                <Button.Visibility>
                    <MultiBinding Converter="{StaticResource ConverterUpdateModel}">
                        <Binding Path="drone.Model" />
                        <Binding Path="droneModel" />
                    </MultiBinding>
                </Button.Visibility>
            </Button>
            <StackPanel x:Name="droneCharge">
                <Button x:Name="sendToCharging" Content="{Binding Path=drone.DroneState,Converter={StaticResource ConvertDroneChargingContent}}"  Visibility="{Binding Path=drone.DroneState,Converter={StaticResource ConvertDroneChargingVisibility}}" Command="{Binding ChargingDroneCommand}"/>
            </StackPanel>
            <StackPanel>
                <Button x:Name="parcelTreatedByDRone"   Visibility="{Binding Path=drone.DroneState,Converter={StaticResource parcelTreatedByDroneVisibility}}" Command="{Binding ParcelTreatedByDrone}">
                    <Button.Content>
                        <MultiBinding Converter="{StaticResource parcelTreatedByDroneContent}">
                            <Binding Path="drone.DroneState" />
                            <Binding Path="drone.Parcel.ParcelState" />
                        </MultiBinding>
                    </Button.Content>
                </Button>
            </StackPanel>-->
        </StackPanel>
    </Grid>
</UserControl>