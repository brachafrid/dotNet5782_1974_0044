<UserControl x:Class="PL.UpdateParcelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   
    <UserControl.Resources>
        <!--<ResourceDictionary>
            <local:ConverterUpdateStation x:Key="ConverterUpdateStation" />
        </ResourceDictionary>-->
    </UserControl.Resources>
    <Grid>
        <StackPanel x:Name="parcel" >
            <TextBlock Text="Id: "/>
            <TextBlock x:Name="Id" Text ="{Binding parcel.Id, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <TextBlock Text="Piority: "/>
            <TextBlock x:Name="Piority" Text ="{Binding parcel.Piority, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <TextBlock Text="Weight: "/>
            <TextBlock  Text ="{Binding parcel.Weight, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <TextBlock Text="Customer Sender: "/>
            <TextBlock  Text ="{Binding parcel.CustomerSender.Name, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <TextBlock Text="Customer Receives: "/>
            <TextBlock  Text ="{Binding parcel.CustomerReceives.Name, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
            <Button Content="close parcel"/>
        
            <Button Content="delete parcel" Command="{Binding DeleteParcelCommand}"/>

        </StackPanel>
    </Grid>
</UserControl>